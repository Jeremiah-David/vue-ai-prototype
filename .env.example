# Copy this file to .env and fill in your values

# OpenAI Configuration
VITE_OPENAI_API_KEY=your_openai_api_key_here

# Development Settings (Optimized for Cost)
VITE_AI_MODE=real
VITE_AI_MODEL=gpt-4o-mini              # Cheapest model with great performance
VITE_AI_TEMPERATURE=0.7                # Balanced creativity/consistency
VITE_AI_MAX_TOKENS=300                 # Limit response length
VITE_AI_CONVERSATION_LIMIT=5           # Limit conversation history

# Instructions:
# 1. Copy this file to .env: cp .env.example .env
# 2. Get your API key from: https://platform.openai.com/api-keys
# 3. Replace 'your_openai_api_key_here' with your actual key
# 4. Set a monthly budget limit on OpenAI dashboard ($5-10 recommended)
# 5. The app will work in mock mode without an API key

# Security Note: 
# Your .env file is in .gitignore and will NOT be committed to git
# Never share your actual API key publicly
